.env
__pycache__/
*.pyc
.ipynb_checkpoints/
dbt_packages/
target/
logs/
data/*
!data/.gitkeep
dbt_transform/profiles.yml

# GX suite/validation-definition/checkpoint JSON is fully regenerated by
# notebooks/olist_GX.ipynb on every run (including a fresh random id per
# expectation even with no logical change), so committing it would just be
# diff noise. gx/great_expectations.yml stays tracked - its ids are stable.
gx/expectations/
gx/validation_definitions/
gx/checkpoints/
